/**
 * matchmaking 設定 (定数)。
 * signalingUrl: MOMO Works 共通のシグナリングサーバ (Render.com hosted)。
 * gameType: サーバ側で部屋を種別分けするタグ。Shogi は 'shogi' を用いる。
 */

export const SIGNALING_URL = 'wss://momo-server-reversi.onrender.com';
export const SHOGI_GAME_TYPE = 'shogi';
